# 55CINE 오오극장 — 디자인 시스템 검토

문서 기준: `designsystem.html` 작성 시점(2026-05) 코드베이스 스냅샷.  
목적: 현재 구현에서 **시스템으로 정리된 것**과 **벗어나거나 개선이 필요한 것**을 구분한다.

---

## 1. 잘 정리된 부분

| 영역 | 내용 |
|------|------|
| **셸 레이아웃** | `ti-shell` + 821px 기준 2열/1열 전환, GNB·시간표·푸터 슬롯 분리가 일관됨 |
| **페이지 번들** | `css/README.md`에 타입별 CSS 로드 순서가 문서화됨 |
| **서브 페이지 헤더** | `section-page-head` + `section-page-subnav` 마크업·클래스가 오오극장·매거진에서 공유됨 |
| **TI 서브 토큰** | `page-shell.css`의 `--ti-*`로 5개 오오극장 페이지 배경·본문·보더가 통일됨 |
| **site-common 토큰** | 매거진·상세 본문용 `:root` 골드 팔레트가 한 파일에 모여 있음 |
| **자산 경로** | `site-root.js` + `TiSiteRoot.resolve()` — 운영 루트 `/`, 서브경로 배포도 지원 |

---

## 2. 이중(삼중) 디자인 언어 — 가장 큰 구조 이슈

사이트는 사실상 **서로 다른 UI 언어**가 공존한다.

| 레이어 | 스타일 | 대표 파일 |
|--------|--------|-----------|
| A. site-common | 웜 골드, `border-radius` 12~14px, pill 서브네비 | `components/site-common.css` |
| B. TI 셸·오오극장 | 그레이, **radius 0**, 각진 버튼·카드 | `shell.css`, `page-shell.css`, `membership.css` … |
| C. 인트로 | 다크 `#070707`, Syne 등 디스플레이 폰트 | `intro.css` |
| D. 영화 상세 | site-common 로드 후 **그레이로 전면 오버라이드** | `movie-detail-theme.css` |

**개선 제안**

- “브랜드 기본 = A” vs “운영 UI = B”를 문서에 명시하고, 신규 페이지는 둘 중 하나만 선택하도록 규칙화.
- 영화 상세(D)는 B와 통합하거나, A를 유지할지 의도적으로 결정 후 `movie-detail-theme` 오버라이드 범위 축소.

---

## 3. 색상 토큰 불일치

### 3.1 골드/액센트 3종 병존

| 값 | 사용처 |
|----|--------|
| `#f0ab2a` (`--brand-gold`) | site-common accent |
| `#5c4a0f` (`--ti-accent`) | page-shell, 링크 강조 |
| `#9a7b1a` (하드코딩) | membership, daegwan, theater-info lead 상단 보더, 선택된 멤버십 카드 |

→ 시각적으로 비슷하지만 **CSS 변수로 연결되지 않음**. 한 토큰(예: `--ti-highlight-border`)으로 통합 권장.

### 3.2 그레이 스케일 분산

- 셸: `#e8e8e8`, `#f2f2f2`, `#bdbdbd`, `#c8c8c8`, `#111`
- TI 텍스트: `#1a1a1a`, `#0a0a0a`, `#3d3d3d`, `#444`
- 상영작 카드: `#b0b0b0`, `#d4d4d4`

→ `--ti-border`와 shell 보더 색이 같아 보이지만, 파일마다 **리터럴 반복**이 많아 유지보수 비용이 큼.

### 3.3 서브네비 “현재” 스타일 2종

- site-common: `var(--brand-ink)` 배경 + **pill** (`border-radius: 999px`)
- TI 오버라이드(`section-page-head.css`): `#1a1a1a` 배경 + **radius 0**

→ 같은 `is-current` 클래스인데 **모양이 다름**(매거진 vs 오오극장). 의도된 차이면 문서화, 아니면 하나로 통일.

---

## 4. border-radius 정책 혼재

| 정책 | 적용 범위 |
|------|-----------|
| 0 (각짐) | GNB, 시간표, np-card, 멤버십, 대관, 상영 상세, 대부분 TI |
| 12~14px | site-common 카드, 매거진 상세 이미지, 서브네비(비-TI) |
| 5px | membership 문서 링크·제보 버튼 등 **예외적 소량 라운드** |
| 50% | 시간표 포스터, 일부 아이콘 |

**개선 제안**

- TI 존: `--ti-radius: 0` 명시 + 예외(5px) 제거 또는 `--ti-radius-sm: 5px`로 토큰화.
- site-common 존: 기존 `--radius` 유지.

---

## 5. 타이포그래피

| 이슈 | 설명 |
|------|------|
| 폰트 로드 분리 | 본문은 Spoqa(CDN), 디스플레이는 Google Fonts — FOUT/개인정보 정책 정리 필요 |
| 크기 체계 비공식 | clamp·rem 혼용, **타입 스케일 표 없음** (0.62rem~2rem 산재) |
| justify 남용 | theater-info, daegwan, membership, osinneun-gil 본문 — 가독성·모바일 줄깨짐 검토 |
| intro 전용 | `ti-breathe` 애니메이션·Syne — 다른 페이지와 공유 안 함 (의도적이면 OK) |

---

## 6. 컴포넌트·패턴

### 6.1 중복·유사 컴포넌트

- **CTA 링크**: `ti-mbr-cms-link`, `ti-dg-form-link`, `ti-mbr-acc__doc-link` — 스타일 유사, 공통 `.ti-cta` 후보.
- **섹션 카드**: `ti-mbr-static`, `ti-seat-intro`, `ti-dg-steps` — border/shadow 패턴이 비슷함.
- **그라데이션 라인**: membership h3, osinneun-gil h3 — 동일 패턴을 utility 클래스로 추출 가능.

### 6.2 GNB 시간표

- `sessionStorage`로 날짜 유지 — 좋음.
- **조치필요** 테스트 날짜(5/27)가 `week-schedule.js`에 남아 있음 → 배포 전 제거.
- 주간 네비 버튼 문구가 길어 4열 그리드에서 말줄임 — 디자인 시스템에 **최대 글자 수** 가이드 없음.

### 6.3 상영작·상세 URL

- 상세 URL이 `movies/movie-detail.html`로 통합됨 — 좋음.
- `css/README.md` 69행 등 **구 경로 문서** 잔존 → 문서 동기화 필요.

---

## 7. 접근성·UX

| 항목 | 상태 | 제안 |
|------|------|------|
| 포커스 | TI 버튼에 `focus-visible` 일부만 | 공통 `:focus-visible` 규칙 |
| 대비 | TI 본문 #3d3d3d on #f9f9f9 — 대체로 양호 | `#888` 보조 텍스트 구간 spot check |
| 터치 타겟 | 서브네비 모바일 4~5px padding | 최소 44px 높이 검토 |
| `aria` | section-page-head, 시간표 tab role 사용 | 문서화 유지 |

---

## 8. 기술 부채·문서

| 항목 | 설명 |
|------|------|
| `components/` vs `css/components/` | 레거시·신규 분리 — 신규는 `css/components/` 규칙 있으나 혼동 여지 |
| 하드코딩 hex | TI 페이지 CSS에 #9a7b1a, #1a1a1a 등 다수 — 토큰 치환 여지 큼 |
| 테스트 플래그 | `past-playing` `testSpinnerDelayMs`, `week-schedule` 테스트 날짜 |
| PDF 링크 | `membership.html` → `documents/2025-donation-disclosure.pdf` 파일 존재 여부 확인 |
| `daegwan.html` | 「시설 및 장비 안내」h2 중복 가능성 — 마크업 정리 |

---

## 9. 우선순위별 개선 로드맵 (제안)

### P0 — 배포·일관성

1. `week-schedule.js` 테스트 날짜 블록 제거(조치필요 주석 구간).
2. `#9a7b1a` → `--ti-accent` 또는 `--ti-highlight` 단일 토큰으로 치환(멤버십·대관·theater-info).
3. `css/README.md` / `CSS리팩토링전략.md`의 movie-detail 경로를 `movies/movie-detail.html`로 갱신.

### P1 — 디자인 시스템 강화

4. `page-shell.css` 또는 `shell.css`에 TI용 CSS 변수 표를 주석으로 고정하고, 페이지 CSS에서 hex 직접 사용 금지 가이드.
5. CTA·섹션 카드 공통 클래스 1~2개 추출.
6. 서브네비 `is-current` — TI / non-TI 스타일 의도 문서화 또는 통일.

### P2 — 장기

7. site-common vs TI **단일 토큰 파일**(`tokens.css`) 분리 후 각 테마가 import.
8. 타입 스케일(예: `--text-sm`, `--text-base`, `--heading-page`) 정의.
9. `designsystem.html`을 빌드 파이프라인에 연결해 토큰 변경 시 스와치 자동 갱신 검토.

---

## 10. 참고

- 시각 문서: [designsystem.html](designsystem.html)
- CSS 로드: [css/README.md](css/README.md)
- 리팩토링 이력: [CSS리팩토링전략.md](CSS리팩토링전략.md)
